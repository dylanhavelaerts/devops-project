## traefik - EntryPoints
entryPoints:
  web:
    address: ":80"
    http:
      redirections:
        entryPoint:
          to: websecure
          scheme: https
  websecure:
    address: ":443"

providers:
  ## link tot mkcerts
  file:
    filename: /certs/certs.yaml
    watch: true ## check voor updates
  
  docker:
    endpoint: "unix:///var/run/docker.sock"
    exposedByDefault: false
    watch: true ## check voor updates

api:
  dashboard: true